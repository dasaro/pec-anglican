(ns pec-anglican.foo
  ;(:use [anglican runtime emit])
  (:gen-class))

; (defquery foo
;   "query template"
;   (let [x (sample (bernoulli 0.5))]
;     (observe (normal x 1.) 0.8)
;     (predict x)))

(def foo 4)
